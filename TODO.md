# TODO

hukitori の作業メモ。優先度は上から目安（高→低）。

## ゴール

**こすったら見えるデジタルクロック** を完成させる。

| 要件 | 説明 |
|------|------|
| 隠れた時計 | ピクセルグリッドに現在時刻（目標: `HH:MM:SS`）を描画 |
| こすって露出 | こすったマス（と周辺）だけ時刻が見える |
| リアルタイム共有 | 全クライアントで `pos` を共有 |
| 生きたクロック | **1 秒ごと**に時刻を更新し、表示も追従 |

## 方針

- **「答える」フローは作らない** — クイズ・正解入力は対象外
- 旧「ランダム 4 桁」はゴールから外れ、**時刻表示**に置き換える

---

## デジタルクロック（コア）

- [ ] **master に時刻表示をマージ** — ブランチ `cursor/time-display-hhmmss-2d33`（PR #2）
  - `14:30:52` 形式、キャンバス 64px、コロン字形
  - サーバー 1 秒ごと `imagedata` 配信
  - クライアントは差分セル更新（こすり状態維持）
- [ ] マージ後、複数タブ／複数人で「こすって時計が見える」ことを確認
- [ ] タイムゾーン方針を README / AGENTS.md に明記（サーバーローカル or `TZ=Asia/Tokyo`）

## こすり・同期

- [x] `pos` 受信時のハイライト範囲拡張（中心・上下左右 100%、斜め 50%）
- [ ] 複数クライアント間でこすり露出が一貫して見える（`pos` + ハイライト；秒更新との干渉確認）
- [x] デバッグ用自動こすり: フラグ制御 + **蛇行スキャン**（`DEBUG_AUTO_SCRATCH_DEFAULT` / `?debug=0`）

## クライアント UX

- [ ] モバイル向けタッチ操作（`pointerover` 依存の見直し）
- [ ] ローディング UI（現状はコンソール `Loading...` のみ）
- [ ] 本番では `DEBUG_AUTO_SCRATCH_DEFAULT = false` にするか検討

## サーバー・インフラ

- [ ] `server/index.js` の listen ログとポート（3001）を一致
- [ ] 環境変数（`PORT`, `TZ`）のドキュメント
- [ ] `image.json` 再生成手順

## 品質・メンテナンス

- [ ] `generate-image.js` の最小テスト（時刻文字列・秒境界）
- [ ] 未使用ファイル・依存の整理（`design.html`, `_index.html` 等）
- [ ] ESLint / Prettier の要否

## ドキュメント

- [x] `AGENTS.md` / `TODO.md` — ゴールをデジタルクロックに改定
- [x] 「答える」を README / AGENTS から削除
- [ ] README をゴール（デジタルクロック）に合わせて更新
- [ ] アーキテクチャ図または Socket イベント一覧

---

完了した項目は `- [x]` にし、必要なら PR リンクを括弧でメモする。
