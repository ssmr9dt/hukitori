# TODO

hukitori の作業メモ。優先度は上から目安（高→低）。

## 方針

- **「答える」フローは廃止** — 4 桁入力・正解判定・「答える」UI は実装しない。README / AGENTS.md の文言も順次 2 ステップ表現に揃える。

## ゲーム機能

- [ ] クライアントで `pos` 受信時のハイライト範囲を拡張する
  - 上下左右 ±1 マス: **100%**（現行の `tail()` / `alpha` と同強度）
  - 斜め 4 マス: **50%** のハイライト
  - 座標はグリッド `(x, y)` で隣接判定（画面端はクランプまたはスキップ）
- [ ] 複数クライアント間でこすり状態を一貫して同期する（`pos` ブロードキャスト + 上記の範囲ハイライト）

## イメージと時間（現在時刻の表示）

**意図**: ランダム 4 桁ではなく、**今の時刻**をピクセル画像に描画する。可能なら **1 秒ごと**に更新する。

**可否（結論）**: **実装可能**。ただし現状のままでは不可で、下記の変更が必要。

| 項目 | 現状 | 時刻表示に必要なこと |
|------|------|----------------------|
| 表示内容 | `hourSeed` から乱数 4 桁 | `Date` から `HHMMSS` 等の文字列を `drawText` |
| フォント | `gdfont-large.js` は **0–9 のみ** | `:` なしなら追加不要。`12:34:56` 形式なら **コロン字形の追加**またはキャンバス拡張 |
| キャンバス幅 | 48px、1 文字 8px | **6 桁** `HHMMSS` → 48px でぴったり。**8 文字** `HH:MM:SS` → 64px で **はみ出す** |
| 更新 | 1 時間キャッシュ、接続時のみ `emit` | 1 秒 `setInterval` + 全クライアントへ `imagedata` 再配信（または差分更新） |
| こすり状態 | ピクセル色が画像の正解データ | 毎秒フル再描画するとハイライトがリセットされうる → **変わった桁だけ更新**等の検討 |

- [ ] 表示形式を決める（例: `143052` / `1430` / `14:30:52`）
- [ ] `generate-image.js`: 乱数生成をやめ、現在時刻文字列で `generateImageData()`
- [ ] 1 秒更新: `server/index.js` でタイマー → `io.emit("imagedata", …)`（負荷は 48×27 程度なら問題になりにくい）
- [ ] クライアント: 受信時にグリッドを作り直すか、秒桁だけ差し替えるか
- [ ] タイムゾーン: サーバー `Date`（UTC/JST）を README に明記

## サーバー・インフラ

- [ ] `server/index.js` の listen ログと実際のポート（3001 / `PORT`）を一致させる
- [ ] 本番用の環境変数ドキュメント（`PORT` など）を README / AGENTS.md に追記
- [ ] `image.json` の再生成手順を README に記載

## クライアント

- [ ] `index.html` のデバッグ用自動こすりループ（`if (1)` ブロック）をフラグまたはビルド外しで制御
- [ ] モバイル向けタッチ操作の調整（`pointerover` 依存の見直し）
- [ ] ローディング中 UI（現在はコンソール `Loading...` のみ）

## 品質・メンテナンス

- [ ] `npm test` 用の最小テスト（`generate-image.js` の時刻文字列・桁幅・1 秒境界など）
- [ ] 未使用ファイル・依存の整理（`design.html`, `_index.html`, `angular2-twitter-bootstrap` の要否確認）
- [ ] ESLint / Prettier の導入要否を決める

## ドキュメント

- [x] `AGENTS.md` を追加（エージェント向けガイド）
- [x] `TODO.md` を追加（本ファイル）
- [ ] README / AGENTS.md から「答える」を削除し、2 ステップの説明に更新
- [ ] README にアーキテクチャ図または Socket イベント一覧を追記

---

完了した項目は `- [x]` にし、必要なら PR リンクや日付を括弧でメモする。
